# The single typed config object from CLAUDE.md 4.8:
# target column, split axis, split direction, seed.
# Architecture and training hyperparameters stay fixed constants elsewhere,
# not fields here (recipe is frozen per 4.7).
